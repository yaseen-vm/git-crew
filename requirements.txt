# ── Core (always required) ──────────────────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.3.0
crewai>=0.80.0
ag2>=0.11.0
typer>=0.12.0
rich>=13.0.0
typing-extensions>=4.0.0
python-dotenv>=1.0.0

# ── LLM providers — install the one that matches your LLM_PROVIDER ─────────────
#
#   LLM_PROVIDER=groq          pip install langchain-groq          (default, free)
#   LLM_PROVIDER=openai        pip install langchain-openai
#   LLM_PROVIDER=anthropic     pip install langchain-anthropic
#   LLM_PROVIDER=ollama        pip install langchain-ollama        (local, no key)
#   LLM_PROVIDER=azure         pip install langchain-openai
#   LLM_PROVIDER=mistral       pip install langchain-mistralai
#   LLM_PROVIDER=google        pip install langchain-google-genai
#   LLM_PROVIDER=openrouter    pip install langchain-openai
#   LLM_PROVIDER=together      pip install langchain-openai
#
# Install all providers at once:
#   pip install "git-crew[all-providers]"

langchain-groq>=0.2.0          # included by default (Groq is free)
# langchain-openai>=0.2.0
# langchain-anthropic>=0.3.0
# langchain-ollama>=0.2.0
# langchain-mistralai>=0.2.0
# langchain-google-genai>=2.0.0
